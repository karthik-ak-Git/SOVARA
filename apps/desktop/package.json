{
  "name": "@sovara/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Sovara — Sovereign AI Desktop Workbench (rebuilt part by part; backup at apps/desktop1)",
  "type": "module",
  "author": "Sovara",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "typecheck": "tsc --noEmit",
    "build:win": "electron-vite build && electron-builder --win --x64",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^3.1.1"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
