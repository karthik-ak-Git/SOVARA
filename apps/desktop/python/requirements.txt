faster-whisper==1.1.1
flask==3.1.1
flask-cors==5.0.1
waitress==3.0.2
numpy>=1.26.0
requests>=2.31.0
crawl4ai>=0.7.0
# OCR — unlimited models, best quality (RapidOCR + HF TrOCR/Paddle fallback)
rapidocr_onnxruntime>=1.4.0
pillow>=10.0.0
huggingface_hub>=0.25.0
onnxruntime>=1.18.0
# Laya decision sidecar — calibrated System-1 classification for the smart router
# (convaiinnovations/laya, Apache-2.0, ~421M params, text-classification).
# Pinned to the latest stable LTS line; transformers drags torch + tokenizers,
# numpy + onnxruntime already cover the rest of the runtime.
transformers>=4.45,<5
torch>=2.2,<2.5
tokenizers>=0.19
safetensors>=0.4

